# Copie ce fichier en .env et remplis les valeurs
# /!\ Ne JAMAIS commiter le .env, uniquement le .env.example

# Token que n8n devra fournir dans le header Authorization
API_TOKEN=remplace-moi-par-une-longue-chaine-aleatoire

# Mode debug (true en local, false en prod)
FLASK_DEBUG=true
